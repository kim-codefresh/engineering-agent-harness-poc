apiVersion: apps/v1
kind: Deployment
metadata:
  name: agent-harness
  namespace: agent-harness
spec:
  replicas: 1
  selector:
    matchLabels:
      app: agent-harness
  template:
    metadata:
      labels:
        app: agent-harness
    spec:
      serviceAccountName: harness-runner
      containers:
        - name: harness
          image: agent-harness:local
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8000
          env:
            - name: LITELLM_MODEL
              value: "anthropic/claude-haiku-4-5-20251001"
            - name: ANTHROPIC_API_KEY
              valueFrom:
                secretKeyRef:
                  name: llm-credentials
                  key: ANTHROPIC_API_KEY
            - name: GITHUB_TOKEN
              valueFrom:
                secretKeyRef:
                  name: github-credentials
                  key: GITHUB_TOKEN
            - name: GITHUB_REPO
              value: "kim-codefresh/engineering-agent-harness-poc"
            - name: GITHUB_BASE_BRANCH
              value: "feat/full-cve-flow-human-gates-lift-and-shift"
            - name: TARGET_REPO
              value: "kim-codefresh/cf-api-test"
            - name: OPENHANDS_URL
              value: "http://openhands.openhands.svc.cluster.local:3000"
            - name: POSTGRES_URL
              value: "postgresql://harness:harness@harness-postgres:5432/harness"
            - name: LINEAR_WEBHOOK_SECRET
              valueFrom:
                secretKeyRef:
                  name: linear-credentials
                  key: LINEAR_WEBHOOK_SECRET
                  optional: true
            - name: LINEAR_API_KEY
              valueFrom:
                secretKeyRef:
                  name: linear-credentials
                  key: LINEAR_API_KEY
                  optional: true
            - name: LINEAR_WORKSPACE
              valueFrom:
                secretKeyRef:
                  name: linear-credentials
                  key: LINEAR_WORKSPACE
                  optional: true
---
apiVersion: v1
kind: Service
metadata:
  name: agent-harness
  namespace: agent-harness
spec:
  selector:
    app: agent-harness
  ports:
    - port: 8000
      targetPort: 8000
